# 야간 자율 진행 상황

## 세션 정보

- **시작**: 2026-08-29 00:13:44 KST
- **종료**: (진행 중)
- **전체 소요**: (진행 중)
- **베이스 커밋**: `63b09ea` (모바일 투자제안 완료)
- **지시 요약**: Phase 1(성능 A+C) → 커밋 `모의투자 화면 작성` / Phase 2(추천종목 디스코드 전달, `docs/추천종목_디스코드_전달.md`) → 커밋 `투자제안의 디스코드 전달` / Phase 3(웹 배포 코드 `docs/web_deploy_plan.md` §7-B 1~12) → 논리 단위 커밋. `master` 직접 커밋, push 안 함. 전체 걸린 시간 체크.

## 소요 시간 체크포인트

| 시각(KST) | 이벤트 | 경과 |
|---|---|---|
| 00:13:44 | 세션 시작, 진행상황.md 생성 | 0분 |
| 00:29 | Phase 1(A+C) 구현·검증 완료 | ~15분 |
| 00:33 | 커밋 #1 `모의투자 화면 작성` | ~20분 |
| 00:35 | Phase 2(디스코드 전달) 구현·검증 완료 | ~21분 |
| 00:36 | 커밋 #2 `투자제안의 디스코드 전달` | ~22분 |
| 00:43 | Phase 3 items 1~5(프로필/읽기전용/paper 라우팅/JSON 백업) 구현·검증 완료 | ~30분 |
| 00:44 | 커밋 #3 | ~31분 |
| 00:49 | Phase 3 items 6~12(스크립트/Docker/Actions/의존성/PWA/문서) 구현·검증 완료 | ~36분 |
| 00:51 | 커밋 #4·#5 | ~38분 |

## 물어보지 않고 내린 결정

| # | 시각 | 결정 | 이유 | 되돌리는 법 |
|---|---|---|---|---|
| D1 | 00:13 | `master`에 직접 커밋, push 안 함 | 저장소 관례(최근 커밋 모두 master 직접), 사용자도 브랜치 언급 없음 | `git branch` 후 cherry-pick |
| D2 | 00:13 | Phase 2 검증은 디스코드 페이로드 문자열을 로그로 확인만, 실제 채널 발송 안 함 | 외부 발송은 사용자 클릭 테스트 몫, 실채널 스팸 방지 | 사용자가 로컬에서 버튼 클릭 |
| D3 | 00:13 | Phase 3는 코드/스크립트/설정(§7-B 1~12)만, 외부 계정(§7-A)은 인수인계 | Turso/Koyeb/GitHub Secrets는 사용자만 생성 가능 | 해당 없음 |
| D4 | 00:20 | A의 창 크기: 워밍업 135달력일 + FORWARD 180달력일, 재사용 판정 완화 25달력일 | 최장 지표 sma60(≈60거래일). 135-25=110달력일≈75거래일 > 60. FORWARD 180으로 다음날 조회 ~120회 재로딩 없이 흡수 | `proposal_advisor.py` 상수 WARMUP/FORWARD/COVER_SLACK 조정 |
| D5 | 00:22 | C(버퍼 200→135)는 `load_market_dataframe`에 `warmup_days` 인자를 추가하는 방식(기본 200 유지) | 백테스트 등 기존 호출부 무영향. ProposalAdvisor만 `warmup_days=0`으로 자체 워밍업 사용 | 인자 제거 후 원복 |
| D6 | 00:28 | 커밋 #1은 이번 세션 모의투자 배선 전체 + A+C + 문서를 한 커밋으로(사용자 계획서에 명시 없던 CLAUDE.md 변경은 제외) | 사용자가 `모의투자 화면 작성` 한 메시지로 지정 | `git reset` 후 분할 |
| D7 | 00:35 | `docs/추천종목_디스코드_전달.md`는 내용 수정 없이 그대로 커밋(스펙 문서). 구현 결과는 진행상황.md에만 기록 | 그 문서엔 사용자가 직접 넣은 빨강 강조가 있어, 색상 규칙대로 스트립하면 사용자 의도가 훼손됨 | 필요 시 해당 문서에 §9 구현결과 추가 |
| D8 | 00:35 | 디스코드 실발송 미검증(웹훅 URL 비어 있음). 포맷·에러경로·버튼 동작만 로컬 검증 | D2 및 `.env`의 `DISCORD_WEBHOOK_URL`이 빈 값 | 사용자가 `.env`에 웹훅 넣고 버튼 클릭 |
| D9 | 00:34 | 디스코드 발송에 `requests` 사용(urllib 대신), 본문은 `json.dumps(ensure_ascii=False).encode('utf-8')` 바이트로 전송 | `requests`가 이미 의존성. CLAUDE.md 5.3의 "UTF-8 바이트 배열" 요건 충족 | — |

## Phase 1 — 성능 개선 A+C  → 커밋 `모의투자 화면 작성`

- [x] 1a. C 선행 조사: 전 전략 최대 lookback = sma60(60거래일). 그 외 RSI14·볼린저20·S1c shift(3). → 워밍업 200→135달력일 안전
- [x] 1b. A 구현: 넓은 창(워밍업135+FORWARD180 달력일) 1회 로드·8전략 지표 1회 계산, 캐시 키 `(mode, data_version)`, 창 내 슬라이스(`_covers`/`_resolve_dates`)
- [x] 1c. 로컬 검증: **advice/sim 추천 결과 2×7=14건 바이트 단위 동일**(스냅샷 대조). 워크포워드 39스텝 평균 45ms(콜드 3.3s) — 종전 스텝당 수 초 대비 ~70배. HTTP 경유 ~0.27s. unittest 13실패 불변
- [x] 1d. 문서 반영: `모의투자_배선…계획.md` §9, `size_exceptions.md` (색상 규칙 적용)
- [x] **커밋 #1** `모의투자 화면 작성` — (해시는 커밋 로그 참조)

## Phase 2 — 추천종목 디스코드 전달  → 커밋 `투자제안의 디스코드 전달`

- [x] 3a. `DiscordNotifier` 서비스 (`backend/app/services/discord_notifier.py`) — UTF-8 바이트 전송, 웹훅 미설정 시 에러
- [x] 3b. `POST /api/proposal/notify-recommendations` (`routes_paper.py`) — advice 모드 재조립, 종목코드 병합, prop 매도시그널, 조립은 `ProposalNotifyBuilder` 서비스로 위임
- [x] 3c. UI 버튼 `📤 추천종목 전달` PC/모바일(수동매수 왼쪽, 인디고 그라데이션, flex-wrap) + `proposal.js` `notifyRecommendations()` (확인창→전달중→alert→원복)
- [x] 3d. `.env.example` 신규(키만, `DISCORD_WEBHOOK_URL=` 포함). config.py는 이미 필드 보유 → 변경 불필요
- [x] 3e. 검증: 포맷 육안 확인(매수 병합·순위·확률, 총 N개, 빈 케이스 `📭 추천 없음`), msg_len 533/579 <2000. 엔드포인트 HTTP 502 에러경로·브라우저 버튼 클릭→alert 확인. **실발송은 웹훅 없어 미검증(D8)**. 매도블록은 prop 보유·매도신호가 없어 포맷 실물 미시연(코드는 스펙대로 구현)
- [x] **커밋 #2** `투자제안의 디스코드 전달`

## Phase 3 — 웹 배포 코드 (§7-B 1~12)

- [x] 1. 실행 프로필 분기 — `config.APP_PROFILE`(full/web). `app.py` `before_request` 게이트: web이면 `/`,`/backtest`,`/recommendation`,`/proposal` → `/proposal-mobile` 302
- [x] 2. 읽기 전용 모드 — `config.READONLY`. `/api/sync*` 403 차단(가상매매 쓰기는 허용). `index.html` 동기화 버튼 `{% if not readonly %}` 숨김
- [x] 3. paper_* 저장소 연결 분기 — `db_manager.get_paper_session(account_type)`: `prop`+`PAPER_DATABASE_URL` → 전용 엔진, 그 외 메인 폴백. `create_all_tables`가 전용 엔진에도 paper 스키마+`account_type` 마이그레이션. routes_paper 5개 라우트를 (메인=시세 / psession=자산) 2세션으로 분리. **로컬(전용 URL 미설정) 동작 불변 검증**
- [x] 4. 가상매매 JSON 내보내기 — `GET /api/paper-trading/export`(schema_version=1, 계좌+보유+체결), `PaperTradingRepository.export_account`, proposal PC/모바일 `⬇️ 내보내기` 버튼(브라우저 다운로드)
- [x] 5. 가상매매 JSON 불러오기 — `POST /api/paper-trading/import`(스키마 검증→직전상태 backup 반환→전체 교체), `replace_account`, `⬆️ 불러오기` 버튼(파일선택→확인창→백업 자동 다운로드→refresh). 왕복·백업·잘못된 스키마(400) 검증 완료
- [x] 6. DB 다이어트 스크립트 — `deploy/build_lite_db.py`. `data/app.db`(231MB) → `data/app_lite.db`(**20.4MB**), 최근 365일+타깃 종목, paper 스키마만. 실행·검증 완료
- [x] 7. 롤링 증분 갱신 스크립트 — `deploy/roll_lite_db.py`. `DATABASE_URL`을 경량 DB로 강제 후 `run_pipeline(incremental)` → 365일 밖 삭제 → VACUUM. `--skip-collect` 프루닝 경로 검증(수집은 KRX 라이브라 미실행)
- [x] 8. Dockerfile + `.dockerignore` — `requirements-web.txt` 설치, 빌드인자 `LITE_DB_URL`로 Release DB 취득, `gunicorn app:app`, `APP_PROFILE=web`/`READONLY=1` 고정. (Docker 미설치로 이미지 빌드 미실행)
- [x] 9. GitHub Actions `.github/workflows/roll-lite-db.yml` — 평일 cron+dispatch, Release 다운→roll→업로드→Koyeb 훅→Discord. 시크릿 없으면 각 단계 건너뜀. `--skip-collect` 기본(TODO 주석). YAML은 `yaml` 모듈 부재로 육안 검토만
- [x] 10. `deploy/requirements-web.txt` — flask/gunicorn + 런타임 최소. fastapi/uvicorn/pykrx/FDR/psycopg2/aiohttp 제외. 수집기는 lazy-import라 안전
- [x] 11. `deploy/README.md` — 환경변수 표, 스크립트 표, 최초 배포 순서, 미결 항목
- [x] 12. PWA — `static/manifest.webmanifest`(start_url `/proposal-mobile`, standalone) + `proposal_mobile.html` head에 manifest/theme-color/apple-* 메타. 아이콘 미포함(사용자가 PNG 추가)
- [x] **커밋 #3** `배포: 실행 프로필/읽기전용/paper 라우팅 + 가상매매 JSON 백업` (items 1~5)
- [x] **커밋 #4** `배포: DB 다이어트·롤링 증분 스크립트` (items 6~7)
- [x] **커밋 #5** `배포: Dockerfile/Actions/의존성/PWA/문서` (items 8~12)

### Phase 3 검증 결과 요약

- full 프로필: 전체 페이지 200, 추천 n=17, `/api/sync` 허용 — 회귀 없음.
- web 프로필+READONLY: PC 페이지 4종 → `/proposal-mobile` 302, `/api/sync` 403, 추천/포트폴리오/매수 200.
- 경량 DB(20MB)로 web 프로필 구동: proposal-mobile 200, 추천 결과가 full DB와 동일(n=17).
- 가상매매 export→reset→import 왕복 복원, 백업 자동 반환, 잘못된 스키마 400.
- unittest 13건 실패 불변(무관). `deploy/*.py` 두 스크립트에 전용 테스트 없음(규칙상 미작성).

## 커밋 로그

| # | 해시 | 메시지 | 시각 |
|---|---|---|---|
| 1 | `37204e7` | 모의투자 화면 작성 (Phase 1: 배선 전체 + A+C + 문서) | 00:33 |
| 2 | `44fea0b` | 투자제안의 디스코드 전달 (Phase 2) | 00:36 |
| 3 | `ca942db` | 배포: 실행 프로필/읽기전용/paper 라우팅 + 가상매매 JSON 백업 | 00:44 |
| 4 | (아래 커밋) | 배포: DB 다이어트·롤링 증분 스크립트 | 00:51 |
| 5 | (아래 커밋) | 배포: Dockerfile/Actions/의존성/PWA/문서 | 00:51 |

## 검증 결과

(진행 중)

## 사용자 조치 필요 / 인수인계

(진행 중)
