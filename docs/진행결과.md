# 진행 결과 — Phase 0 / Phase 1 / Phase 2 자율 구현

- 시작: 2026-08-30 00:29:31 (KST) / 종료: 2026-08-30 01:05 (KST) / 총 경과: 약 35분
- 대상: [`1, 전략변경계획.md`](1,%20전략변경계획.md) Phase 0·1, [`순수관행.md`](순수관행.md) §8 (Phase 2)
- 결과: **3개 Phase + 사용자 검토 반영 후속까지 코드 구현 + 검증 완료. 5개 커밋.**
  (`bfd5213` Phase 0 / `1fac372` Phase 1 / `5460fa6` Phase 2 / `2ea9899` 문서 / `5ccd9af` 후속)

---

## 진행 상황

- [x] **Phase 0 — CI 데이터 파이프라인 최소화** (`bfd5213`)
  - [x] 2-1 `run_pipeline` `stage="ohlcv-only"` + `collect_target_market_data(ohlcv_only=)` 분기
  - [x] 2-2 `roll_lite_db.py`: `--skip-collect` 제거, `stage=ohlcv-only` 고정, 수집 예외/신규 거래일 0 → `exit 1`
  - [x] 2-3 워크플로 `if: failure()` Discord 경고 스텝
  - [x] 2-4 워크플로 `Roll` 스텝 `ohlcv-only`화, `KRX_ID`/`KRX_PW` 제거
  - [x] 경량 DB 2년치 확장 (`build_lite_db.py`/`roll_lite_db.py` 일수 기본값 730)
- [x] **Phase 1 — 백테스트 청산 규칙 + S1b/S1c 폐기** (`1fac372`)
  - [x] 7-1 `_simulate_trading` -5%/+10% 청산 (elif 손절→익절→전략매도, D-0 종가 체결)
  - [x] 7-2 S1b/S1c 폐기 (`STRATEGY_MAP`/`STRATEGY_COMBOS`, `backtest_run` `[1,2,5,6,7,8]`)
  - [x] 7-3 `index.html` 카드 제목·칩 숨김, `proposal_advisor` 라벨 정리
  - [x] 검증 (unittest 신규 실패 0 / 로컬 백테스트 청산 태그 분포 확인)
- [x] **Phase 2 — 순수관행 §8 적용** (`5460fa6`)
  - [x] 8-2-A `composite_score.CompositeScorer` (팩터 합성 점수·백분위·등수)
  - [x] 8-2-B `purerule_engine.PureRuleEngine` — 백테스트 리더보드 신규 엔트리 `combo_id=22`
  - [x] 8-2-C `_simulate_trading` 매수 정렬 `prob_up` → 합성 점수, 전 백테스트 5슬롯 통일
  - [x] 8-2-D 추천 리스트 합성 점수순 정렬, 매도 시그널에 개시 전략+합성 점수·등수, 헤더 라벨 교체
  - [x] 8-2-E `paper_positions`/`paper_trade_histories` `entry_strategy` 컬럼 + 마이그레이션 + `manual-buy` 저장
  - [x] 검증 (unittest 신규 실패 0 / Flask test client end-to-end)
- [x] **후속 — 사용자 검토 반영** (`5ccd9af`)
  - [x] 수급 팩터(`prob_up`의 `z_investor` 0.4 가중) **완전 제거** — `calc_investor_z_score` 항상 0, `calc_sigmoid_prob` z_core 단독
  - [x] 합성 점수 모멘텀 **6-1개월 → 12-1개월**(이력 부족 구간 6-1개월 폴백)
  - [x] 검증 (unittest 신규 실패 0 / prob_up 범위·합성 점수 산출 확인)

---

## 경과 로그

| 시각 | 경과 | 내용 |
|---|---|---|
| 00:29:31 | 0:00 | 시작. 코드 탐색 |
| 00:31 | 0:02 | 베이스라인 테스트: 45개 중 13 실패(**사전 존재** — `test_04` collector, `test_07` s1/s2/s3/s4 픽스처 드리프트). 이후 "신규 실패 없음"을 검증 기준으로 삼음 |
| 00:33 | 0:04 | **Phase 0 완료**. `run_data_collection.py`, `market_data_collector.py`, `deploy/roll_lite_db.py`, `deploy/build_lite_db.py`, `roll-lite-db.yml`. 구문·CLI 파싱 확인. CI 실제 실행은 로컬 검증 불가. `bfd5213` |
| 00:37 | 0:08 | **Phase 1 완료 + 검증**. backtest_engine 청산 로직, S1b/S1c 폐기, routes_paper 루프, proposal_advisor 라벨, index.html. unittest 신규 실패 0. 로컬 백테스트(app.db, combo 1, 2025~2026): 손절 30(≤-5%)/익절 26(≥+10%)/전략매도 21((-5,+10) 구간). `1fac372` |
| 00:44 | 0:15 | **Phase 2-A/B**. `CompositeScorer` — app.db 대상 검증: 350종목 합성 점수·등수 산출. `PureRuleEngine` — combo 22 백테스트 실행: 298 trades, 태그 분포 정상(랭킹편입/랭킹이탈/정기교체/하드스톱/최대보유일) |
| 00:47 | 0:18 | **Phase 2-C/D/E**. 매수 정렬 합성 점수화 + 5슬롯. proposal_advisor 추천/포트폴리오 뷰. `entry_strategy` 컬럼 + app.db/app_lite.db 마이그레이션. proposal/recommendation 헤더 |
| 00:49 | 0:20 | **Phase 2 검증**. Flask test client: `/api/backtest-run` 성공(리더보드 7엔트리, 순수관행 combo 22 rank 1), `/api/recommended-stocks` composite_pct·rank 정렬 확인. unittest 신규 실패 0. `5460fa6` |
| 01:05 | 0:36 | **후속**. 사용자 검토 반영 — 수급 팩터 제거(`base_strategy`), 12-1개월 모멘텀(`composite_score`). unittest 신규 실패 0, prob_up 10~90 정상. `5ccd9af`. 진행결과.md 정리 |

---

## 검증 결과 요약

| 항목 | 방법 | 결과 |
|---|---|---|
| 전체 unittest | `python -m unittest discover -s backend/tests` | 45개 중 13 실패 — **베이스라인과 동일, 신규 실패 0** |
| Phase 1 청산 규칙 | 로컬 백테스트(app.db) 매도 로그 `profit_pct` 분포 | 손절 전부 ≤ -5%, 익절 전부 ≥ +10%, 전략매도 전부 (-5%, +10%) |
| Phase 1 S1b/S1c 폐기 | `STRATEGY_MAP`/`COMBOS` 검사 | `['S1','S1a','S2','S3','S4','S5']`, combo `[1,2,5,6,7,8,9,10,13,14,17,18,19]` |
| Phase 2 합성 점수 | `CompositeScorer().score()` on app.db | 350종목 점수·백분위·등수 산출, 상위 종목 합리적 |
| Phase 2 순수관행 엔트리 | `PureRuleEngine().run_backtest()` | 리더보드 `combo_id=22` 생성, 매매일지 태그 5종 정상 |
| Phase 2 end-to-end | Flask test client (`app.test_client()`) | `/api/backtest-run` 성공, 리더보드 7엔트리, `/api/recommended-stocks` 합성 점수순 정렬 |
| DB 마이그레이션 | `create_all_tables()` 후 `PRAGMA table_info` | `paper_positions.entry_strategy`, `paper_trade_histories.entry_strategy` 추가 확인(app.db·app_lite.db) |

### 로컬에서 검증 못 한 것 (환경 제약)
- **Phase 0 CI 실제 실행**: KRX/네이버 접근·GitHub Actions·Discord 웹훅이 로컬에 없어 워크플로 실물 실행 미검증. 코드 구문·CLI 파싱·YAML 구조만 확인.
- **Phase 2 프론트 렌더**: 브라우저·구동 서버 없이 템플릿 육안 렌더 미확인. API 응답 스키마(합성 점수 필드 포함)까지만 검증.

---

## 자율 결정 사항 (사용자 확인 없이 진행)

1. **`ohlcv-only` = `stage` 인자 값**으로 구현(별도 불리언 플래그 X). `MarketDataCollector.collect_target_market_data(ohlcv_only=)` 파라미터 추가, 기존 `_fetch_fdr_fallback`를 정식 경로로 승격. KRX rate-limit `sleep(0.5)`는 `ohlcv_only`일 때 생략.
2. **`roll_lite_db.py` 실패 판정**: 수집 중 예외 발생 **또는** `investor_trading_daily` 최신일이 안 늘어남 → `SystemExit(1)`. 워크플로는 `if: failure()`로 Discord 경고 + 기존 Release 자산 보존.
3. **경량 DB 2년치**: `--days` 기본값 `365 → 730` (`build_lite_db.py`, `roll_lite_db.py` 둘 다, 프루닝 창 포함). 이미지/Render 용량 여유 판단.
4. **STOP_LOSS_PCT/TAKE_PROFIT_PCT를 `backtest_engine` 모듈 상수로** 정의하고 `proposal_advisor`가 import — 순환 import 회피 + 단일 소스. 값(-5/+10)은 기존 그대로.
5. **`STRATEGY_COMBOS` 정리**: S1b/S1c 포함 8개(3·4·11·12·15·16·20·21) 제거, 남은 13개는 **번호 유지(결번)**. `backtest_engine` 상단 S1b/S1c import 라인도 제거(클래스 파일은 존치).
6. **순수관행 백테스트 엔트리(`combo_id`)**: **22** 사용(1~21과 충돌 없는 새 번호). `run_backtest_for_combo`가 아닌 **별도 모듈** `purerule_engine.PureRuleEngine`로 분리 — `backtest_engine` 비대화 방지 + 시뮬 규칙이 근본적으로 다름.
7. **순수관행 v1 사이징 = 균등 비중** → **사용자 검토 결과 확정(OK)**. 변동성 타깃 `w∝1/σ` + ±5%p 밴드 + "비중조정" 행은 v2 후보로 남김(필수 아님). → [`순수관행.md`](순수관행.md) §8-2-B의 "더 단순한 대안".
8. **순수관행 청산 규칙 구체값**: ATR(14) 하드스톱 배수 **2.5**, 최대 보유일 **30거래일**, 랭킹 이탈 버퍼 **2N(=10위)**, 리밸런싱 주기 = ISO 주차가 바뀌는 마지막 거래일. **트레일링 스톱(고점−3·ATR)은 v1 미구현 → v2에서 추가 예정**(아래 후속작업).
9. **합성 점수 모멘텀** → **사용자 검토 결과 12-1개월(252-21거래일)로 변경**(`5ccd9af`). 이력 부족 구간은 6-1개월(126-21)로 폴백. 팩터 패밀리: 모멘텀/추세품질/저변동/거래량(OBV 20일변화) 균등 1.0, 단기역추세 0.1. 윈저 1/99, 횡단면 z, 섹터 중립.
10. **유동성 게이트**: 가격 ≥ 1,000원 **and** 20일 평균 거래대금 ≥ 3억. 부적격 종목은 랭킹 최하위로 밀되 백분위·등수는 NaN.
11. **`prob_up` 표시 필드 처리**: 값을 제거하지 않고 **합성 백분위로 덮어씀**(추천 응답·매매일지 저장 필드 공통). 별도 `composite_pct`/`composite_rank` 필드도 추가. **수급 항(`z_investor`)은 사용자 검토 결과 지금 제거함**(`5ccd9af`) — `calc_sigmoid_prob`가 z_core 단독. 로컬·웹 `prob_up` 일치.
12. **화면 라벨**: `proposal.html`/`recommendation.html` 헤더 "상승 확신 확률 (%)" → "합성 점수 (백분위)". `backtest.html` 매매일지에는 해당 컬럼이 없어 변경 없음. `proposal.js`는 `r.prob_up` 값을 그대로 렌더(이제 합성 백분위) — **등수 별도 노출은 미반영(후속 JS 작업)**.
13. **`manual-buy` 전략 태그**: 요청 body의 `strategy`(또는 `entry_strategy`) 값을 저장, 없으면 `"MANUAL"`. 추천 카드 → 매수 연동 시 프론트가 전략 키를 함께 보내면 자동 기록됨(프론트 전달부는 후속).
14. **파일 크기**: `purerule_engine.py` 206줄로 CLAUDE.md 200줄 기준을 6줄 초과. `backtest_engine.py`(462줄, 사전 초과)와 함께 후속 리팩터 대상. 자율 세션에서 무리한 분할보다 동작 검증 우선.
15. **`git` 커밋 대상**: Phase별로 소스만 스테이징(생성 산출물 `data/`, `test_07_..._results.csv`는 제외). 브랜치 분기 없이 `master` 직접 커밋(저장소가 solo·master 워크플로).

---

## 후속 작업 (코드)

### A. 확정 — 더 안 건드림
- **순수관행 사이징 = 균등 비중** (자율결정 #7, 사용자 OK). v2 옵션으로만 남김.
- **수급 팩터 제거 완료** (`5ccd9af`).
- **12-1개월 모멘텀 적용 완료** (`5ccd9af`, 6-1개월 폴백).
- **D(경량 DB 3년치 확대) — 안 함**. 추천은 항상 최신 기준일이고 2년치(≈500거래일)면 최신일의 12-1개월 룩백(273거래일)이 이미 커버됨. 3년치는 "웹에서 2년 과거 백테스트 전 구간 12-1개월"을 원할 때만 필요한데 웹은 백테스트를 안 함. 경량 DB는 730일(2년) 유지.
- **F(파일 크기) — `docs/(완) size_exceptions.md`에 사유 기록 완료**. `purerule_engine.py` 206줄(6줄 초과, 정적 헬퍼 6개로 이미 분할). 무리한 분할보다 유지.
- **E(프론트) — 이번 세션에 처리 완료**. 아래 "E 완료 내역" 참조.

### B. v2 예정 — 트레일링 스톱 (자율결정 #8)
- `purerule_engine.PureRuleEngine._exit_reason` 에 트레일링 스톱 추가.
  - 포지션 dict 에 `peak`(보유 중 최고 종가) 필드 추가, 매일 갱신.
  - 청산 우선순위: 하드스톱 → **트레일링(고점 − 3·ATR14)** → 랭킹이탈 → 최대보유일 → 정기교체.
  - 상수 `TRAIL_MULT = 3.0`.
- [`순수관행.md`](순수관행.md) §3-5·§3-6 와 일치시킴.

### C. v2 예정 — 순수관행 변동성 타깃 사이징
- 균등 비중 → `w_i ∝ 1/σ_i` (60일 실현변동성), 포트 사전변동성 연 12~15% 스케일.
- 주간 리밸런싱 시 ±5%p 밴드 밖만 재조정 → 부분 매수/매도를 `strategy_tag="비중조정"` 행으로 기록(기존 `strategy_trade_logs` 스키마 유지).

### E 완료 내역 (`static/js` + 템플릿, 이번 세션)
- `static/js/proposal.js` `renderRecommendations`: `${r.prob_up}%` 옆에 `${r.composite_rank}위` 표기.
- `static/js/proposal.js` `renderSellSignals`: 매도 시그널 행/카드에 `매수: {entry_strategy} · 합성 {pct}% · {rank}위` 메타 라인 추가.
- `static/js/proposal.js` 매수 배선: `data-buy` 5번째 필드에 `r.strategy` 실어 `openBuyModal` → `buyStrategy` 보관 → `confirmBuy` 가 `manual-buy` body에 `strategy` 전달(추천 카드 매수 시 자동 태그, 수동 입력 시 `MANUAL`).
- `templates/backtest.html`: 하드코딩된 `<option value="S1c">…` 3개 제거(리더보드는 JS가 API로 채움).
- **미처리**: `static/js/chart.js` 의 S1b/S1c 오버레이 계산 코드 — 대시보드 칩이 CSS `display:none`이라 토글 불가 = **기능상 무해(inert)**. 실제 삭제는 브라우저 검증 필요해 후속.

---

## ★ 사용자가 직접 해야 할 일 — 테스트 & 배포 (순차)

> 로컬에서 대신 못 한 것: KRX/네이버 접근, GitHub Actions 실행, Discord 웹훅, 브라우저 렌더, Release 자산 교체, Render 재배포.

### 1단계 — 로컬 앱 스모크 테스트 (5분)
1. `python app.py` 로 서버 기동.
2. 브라우저에서 확인:
   - **시뮬레이션**(`/backtest`): "백테스트 실행" → 리더보드에 **7개 전략**(순수관행 + S1·S1a·S2·S3·S4·S5)이 뜨는지. S1b/S1c 없는지.
   - **투자제안**(`/proposal`) / **모의투자**(`/recommendation`): 추천 목록이 뜨고 "합성 점수 (백분위)" 열에 값 + `N위` 표기가 보이는지. S1b/S1c 카드 없는지.
   - **메인 대시보드**(`/`): 차트 카드 제목이 "기술적 매매지표 (S1~S5)", 전략 칩에 S1b/S1c 없는지.
   - 모의투자에서 **추천 카드의 🛒 매수** 1건 → 보유 목록 표시 확인. 이후 매도 신호가 뜨는 종목 카드에 `매수: S3 · 합성 42% · 87위` 같은 메타 라인이 보이는지.
   - (선택) `sqlite3 data/app.db "SELECT stock_code, entry_strategy FROM paper_positions"` 로 `entry_strategy`에 `S3` 등 전략 키가 저장됐는지(수동 입력이면 `MANUAL`).
3. 이상 없으면 다음 단계.

### 2단계 — 경량 DB 재생성 & Release 자산 교체 (10분)
> 웹서버가 새 스키마(`entry_strategy` 컬럼)와 2년치 데이터를 받으려면 필수.
1. 로컬에 최신 `data/app.db` 준비(평소 "수급 동기화"로 갱신된 상태).
2. `python deploy/build_lite_db.py` 실행 → `data/app_lite.db` 가 **2년치(730일)**로 재생성됨(파일 ~40MB 예상).
3. GitHub → 저장소 → **Releases → `lite-db` 태그** → 기존 `app_lite.db` 자산 삭제 후 새 파일 업로드
   (또는 `gh release upload lite-db data/app_lite.db --clobber`).

### 3단계 — CI 워크플로 실물 검증 (5분 + 대기)
1. GitHub → **Actions → `roll-lite-db` → Run workflow**(`workflow_dispatch`) 수동 실행.
2. 로그에서 확인:
   - `stage=ohlcv-only` 로 FDR 지수 + 타깃 OHLCV 수집되는지 (KRX 로그인 시도 없이).
   - "신규 거래일 확인" 통과 → 730일 프루닝/VACUUM → Release 업로드 → Render 재배포 훅 → Discord 결과 알림.
   - 실패 시: "⚠️ roll-lite-db 실패" Discord 경고가 오고 Release 자산이 보존되는지.
3. `secrets` 확인: `RENDER_DEPLOY_HOOK_URL`, `DISCORD_WEBHOOK_URL` 만 필요. `KRX_ID`/`KRX_PW` 는 이제 안 씀(삭제해도 됨).

### 4단계 — 웹 배포 확인 (5분)
1. 2·3단계 후 Render가 재배포되면 `https://algofinder-vrrs.onrender.com/proposal-mobile` 접속.
2. 추천 목록이 뜨고 합성 점수가 표시되는지. (수급 NULL이어도 `prob_up`/합성 점수가 로컬과 동일 로직으로 나옴.)
3. Turso `prop` 계좌에 `entry_strategy` 컬럼 마이그레이션이 앱 기동 시 자동 적용됨(`create_all_tables`) — 첫 접속 후 모의매수 1건으로 확인.

### 5단계 — git push
- 로컬 스모크(1단계)까지 OK면: `git push origin master` (커밋 5개: `bfd5213`~`5ccd9af`).
- push 전에는 배포에 반영 안 됨. 2~4단계는 push 후 진행해도 되고, Release 자산 교체(2단계)는 push와 무관하게 먼저 해도 됨.

### 참고 — 기존 테스트 13건 실패
`test_04`(파라미터 픽스처 노후, 이번에 `ohlcv_only` 추가로 1건 더 미커버), `test_07` s1/s2/s3/s4(정답 CSV 데이터 드리프트) — **이번 작업 이전부터 실패하던 것**. 고치려면 "테스트 코드 작성해" 지시 후 fixture 갱신 필요.
