# 진행 결과 — Phase 0 / Phase 1 / Phase 2 자율 구현

시작: 2026-08-30 00:29:31 (KST)
대상: [`1, 전략변경계획.md`](1,%20전략변경계획.md) Phase 0·1, [`순수관행.md`](순수관행.md) §8 (Phase 2)

---

## 진행 상황

- [x] Phase 0 — CI 데이터 파이프라인 최소화
  - [x] 2-1 `run_pipeline` `stage="ohlcv-only"` 추가 + `collect_target_market_data(ohlcv_only=)` 분기
  - [x] 2-2 `roll_lite_db.py` 변경 (`--skip-collect` 제거, `stage=ohlcv-only`, 수집 실패/신규일 0 → exit 1)
  - [x] 2-3 워크플로에 `if: failure()` Discord 경고 스텝
  - [x] 2-4 워크플로 `Roll` 스텝 `ohlcv-only`화, `KRX_ID`/`KRX_PW` 제거
  - [x] 경량 DB 2년치 확장 (`build_lite_db.py`/`roll_lite_db.py` 일수 기본값 730)
- [ ] Phase 1 — 백테스트 청산 규칙 + S1b/S1c 폐기
  - [ ] 7-1 `_simulate_trading` -5%/+10% 청산
  - [ ] 7-2 S1b/S1c 폐기 (STRATEGY_MAP/COMBOS, backtest_run 루프)
  - [ ] 7-3 5개 화면 반영 (index.html, proposal_advisor 라벨)
  - [ ] 검증 (unittest + 백테스트)
- [ ] Phase 2 — 순수관행 §8 적용
  - [ ] 8-2-A 합성 점수 공유 서비스
  - [ ] 8-2-B 백테스트 순수관행 엔트리
  - [ ] 8-2-C S1~S5 매수 정렬 → 합성 점수, 슬롯 5 통일
  - [ ] 8-2-D 화면 정렬·표시
  - [ ] 8-2-E `paper_positions.entry_strategy` 컬럼
  - [ ] 검증

---

## 경과 로그

| 시각 | 경과 | 내용 |
|---|---|---|
| 00:29:31 | 0:00 | 시작. 코드 탐색 |
| 00:31 | 0:02 | 베이스라인 테스트: 45개 중 13 실패(사전 존재 — test_04 collector, test_07 s1/s2/s3/s4 픽스처 드리프트). 이후 "신규 실패 없음"을 기준으로 검증 |
| 00:33 | 0:04 | **Phase 0 코드 완료**. `run_data_collection.py`, `market_data_collector.py`, `deploy/roll_lite_db.py`, `deploy/build_lite_db.py`, `.github/workflows/roll-lite-db.yml`. 구문·CLI 파싱 확인. CI 실제 실행은 로컬 검증 불가 |

---

## 자율 결정 사항

(작업 중 결정한 내용을 여기 누적)

---

## 최종 결과

(완료 후 작성)
