# 프로젝트 AI 코딩 규칙 (Claude Code용)

이 문서는 `AI 코딩규칙.txt`를 기본으로 하고, `.agents/AGENTS.md`에만 있던 보완 규칙(테스트 넘버링, 테스트 동기화 조건)을 추가로 반영한 Claude Code 행동 강령입니다. 아래 규칙을 예외 없이 엄격하게 준수해야 합니다.

## 1. 진행 및 응답 규칙 (가장 중요)

- **명시적 지시 전 코딩 절대 금지**: 사용자가 **"수정해"**, **"그대로 진행해"**, **"작업시작해"**라고 명시적으로 지시하기 전까지는 절대로 임의로 소스 코드를 작성/수정하지 않는다.
  - 항상 먼저 구체적인 구현/수정 계획(Plan)을 제시하여 사용자의 사전 승인을 받은 후에만 코딩 단계로 진입한다.
  - 코딩 완료 후에는 테스트 코드를 실행하여 원하는 결과가 나오는지 확인하고 그 결과를 함께 보고한다.
- **명시적 지시 전 실행 절대 금지**: 사용자가 **"실행해"**라고 지시하기 전까지는 테스트 코드 실행이나 시스템 명령 실행을 하지 않는다.
- **질의응답 및 원인 분석 시 수정 금지**:
  - "~할 수 없어?"와 같은 가능성/방법 문의에는 실제 코드를 작성하지 않고 **적용 가능한 방법과 방향성만 텍스트로 제시**한다.
  - "왜 그렇지?", "원인 파악해", "다시 확인해봐" 등 원인 분석 요청에는 **원인 분석 결과와 제안 수정안만 텍스트로 제시**한다.
  - 반영은 사용자가 제안을 검토하고 선택·지시한 이후에만 시작한다. 임의 추측으로 정답(기대값) 기준을 단정하지 말고, 모를 때는 사용자에게 먼저 질문해 확인한다.

## 2. 개발 원칙

### 규칙 1 — 역할 분담 및 Thin Client
- 프론트엔드(HTML/JS)는 데이터 렌더링 및 사용자 입력 중계만 담당한다.
- 모든 비즈니스 연산, 데이터 가공, 유효성 검증은 백엔드(Python) 객체가 전담하며, 인터페이스는 RESTful HTTP API(JSON)를 이용한다.
- Flask 앱의 진입점은 `app.py` 팩토리 파일 하나로 둔다. REST 엔드포인트는 `app.py`에 직접 정의하지 않고, `backend/app/api/routes_*.py`에 도메인별 Blueprint로 분리한 뒤 `app.py`에서 등록(`register_blueprint`)한다. (`main.py` 단일 파일에 모든 라우트를 몰아넣지 않는다.)

### 규칙 2 — 명세 및 주석 표준
- 모든 파이썬 모듈, 클래스, 함수(메서드) 시작 부분에 한글 독스트링(Docstring)으로 목적, 매개변수(types/description), 반환값(returns)을 명확히 정의한다.
- 복잡한 비즈니스 로직 구간 및 주요 조건 분기점에는 상세한 인라인 한글 주석을 작성한다.

### 규칙 3 — 주 객체 중심 모듈화 및 크기 제한
- 모든 파이썬 소스 파일(`.py`)은 하나의 명확한 **주 객체(Main Class 1개)**를 중심으로 구성한다.
- 해당 주 객체 내부에서만 직접 사용되는 보조 요소(전용 예외 클래스, 데이터 클래스 등)만 동일 파일에 제한적으로 포함할 수 있다.
- 파일명은 해당 주 객체의 역할과 책임을 명확히 드러내도록 명명한다.
- (비대화 방지) 주석/독스트링을 포함한 단일 파일의 전체 길이는 200줄 이내, 개별 메서드는 30줄 이내를 원칙으로 하며, 초과 시 역할을 세분화해 새 파일로 분리한다. 불가피하게 기준을 초과해야 하는 경우, 초과 사유를 `docs/size_exceptions.md`에 파일별로 기록하고 작업 완료 후 사용자 검토를 받는다.

### 규칙 4 — UI 레이아웃 및 공통 사이드바 규격
- (진입 분기) `index.html` 접속 시 스크립트로 기기(PC/모바일)를 판별하여 각 환경의 첫 화면으로 자동 이동한다.
- **PC 환경**: `frontend/pages/*.html` 다중 페이지로 분리하며, 좌측 사이드바 + 우측 메인 콘텐츠 2분할 레이아웃을 기본으로 한다.
  - 레이아웃 시프트 방지를 위해 `frontend/css/common.css`에서 `<aside id="sidebar-container">`의 너비를 `width: 220px; flex-shrink: 0;`로 고정한다.
  - **사이드바는 서버 템플릿 include 방식(예: Jinja2 `{% include %}`)으로 공통 관리한다.** 최초 HTML 응답에 완성된 사이드바를 포함해 내려보내며, JS로 페이지 로드 후 사후 주입하지 않는다 — JS 주입 방식은 "빈 사이드바가 보였다가 채워지는" 콘텐츠 플래시(깜박임)를 유발하므로 사용하지 않는다.
  - 활성 메뉴(`active`) 강조는 서버가 현재 요청 경로(예: Flask `request.path`)를 기준으로 렌더링 시점에 결정한다. 클라이언트 JS로 사후 판별하지 않는다.
- **모바일 환경**: `frontend/mobile/pages/*.html` 다중 페이지로 분리하며, 상단 헤더 + 하단 탭바 네비게이션을 기본 레이아웃으로 한다.
  - 공통 네비게이션(상/하단 바)은 `frontend/mobile/components/nav.html` 단일 파일로 분리하고 `load_mobile_nav.js`로 동적 주입한다.
  - 모바일 터치 환경에 맞춘 `m_common.css` 스타일을 적용한다.
- **공통**: HTML 내 CSS/JS 참조는 서버 배포 시 경로 오류 방지를 위해 상대경로(예: `../css/common.css`, `../js/api.js`)를 표준으로 사용한다.

### 규칙 5 — 데이터 주도 입출력 검증 및 매개변수 완전성
- 소스 코드(`backend/app/.../*.py`)와 단위 테스트(`backend/tests/.../test_*.py`)는 1:1로 매칭한다.
- 테스트용 입력값과 기대 출력값은 하드코딩하지 않고 `backend/tests/fixtures/` 폴더 아래 별도의 데이터 파일(JSON/TXT)로 분리 저장하며, 복수의 테스트 케이스 목록을 구성한다.
- 기본값(`default`)이 존재하는 매개변수라도 테스트 데이터의 `input` 필드에는 모든 매개변수를 생략 없이 명시한다.
- 모든 단위 테스트 코드는 실행 전 `inspect` 모듈로 대상 메서드가 요구하는 모든 파라미터가 `input`에 기재되어 있는지 자동 검증(Assert)한 뒤, `[입력값 | 예상값 | 실제 실행 결과]`를 터미널에 대조 출력한다.
- **테스트 파일/케이스 분류**: 백엔드 주요 객체별로 1:1 전용 테스트 파일 및 전용 Fixture를 분리하고, 테스트 케이스에는 순번(`test_01_...`)을 표기하여 사용자가 특정 케이스만 손쉽게 실행 지시할 수 있도록 한다.
- **Fixture 정답(Truth) 데이터 생성 스크립트**: 픽스처의 정답 데이터를 코드로 생성해야 하는 경우, 생성 스크립트는 `backend/tests/fixtures/` 폴더 내에 두되 파일명은 결과물인 정답 데이터 파일과 동일한 베이스 이름(확장자만 다름, 예: `test_07_xxx_truth.py` → `test_07_xxx_truth.json`)으로 작성한다.

### 규칙 6 — 배치 연산, 외부 시스템 연동 및 알림 (후순위 — 프로젝트 마무리 단계에서 구현)
- 본 규칙은 배치 처리/외부 알림 기능이 실제 요구사항으로 확정되는 프로젝트 마무리(배포 직전) 단계에 적용한다. 그 전까지 `BatchProcessor`/`DiscordNotifier`가 없거나 관련 설정값(`DISCORD_WEBHOOK_URL` 등)이 미사용 상태인 것은 규칙 위반으로 감사(audit)하지 않는다.
- 대량 데이터 처리 및 주기적 배치 연산은 `backend/app/services/batch_processor.py`의 `BatchProcessor` 클래스가 전담하며 웹 UI와 완전히 독립적으로 동작한다.
- (작업 설정 형식) 배치 작업 설정 데이터(대상, 파라미터, 필터링 조건 등)는 JSON 또는 구조화된 Text 파일로 정의하며, `BatchProcessor`는 이 설정 파일을 읽어 작업을 실행하거나 Spark 등 외부 시스템으로부터 전달받아 처리한다.
- (외부 인터페이스) Spark 등 외부 시스템과의 주기적 연동을 위해 전용 REST API 엔드포인트(`/api/batch/...`)를 제공하고 JSON 통신을 수행한다.
- (결과 알림) 배치 작업 완료/에러 발생 시 처리 요약 데이터를 `backend/app/services/discord_notifier.py`의 `DiscordNotifier` 객체를 통해 디스코드 채널로 자동 발송한다.

### 규칙 7 — 보안 및 배포 환경 최적화
- 웹 프레임워크는 PythonAnywhere(WSGI) 환경과 완벽히 호환되는 Flask 기반 REST API를 사용하며, DB는 `data/app.db`(SQLite)를 표준으로 사용한다.
- 디스코드 웹훅 URL, DB 접속 정보, Spark 통신용 API 시크릿 키 등 민감한 정보는 `.env` 파일로 격리 관리하며 `.gitignore`에 등록해 외부 노출을 방지한다.

## 3. 추가 규칙

- **새 프로젝트 생성 시**: 항상 `.venv` 가상환경을 프로젝트 폴더 내에 생성하고 진행한다.
- **소스 변경 시 테스트 코드 동기화**: 소스가 변경되면 원칙적으로 테스트 코드도 함께 갱신해야 하지만, 규칙 1(명시적 지시 전 코딩 금지)에 따라 사용자가 **"테스트 코드 작성해"**라고 명시적으로 요청할 때만 해당 단위 테스트 코드 및 Fixture 데이터를 생성·갱신한다. 명시적 지시 전에는 임의로 테스트 코드를 생성하지 않는다.

## 4. 프로젝트 표준 폴더 구조

```text
my_project/
│
├── app.py                        # Flask 앱 팩토리 및 서버 진입점 (Blueprint 등록)
├── backend/                      # [파이썬 백엔드] 비즈니스 로직 및 DB 연동 전담
│   ├── app/
│   │   ├── core/                 # DB 연결 및 전역 설정 (config.py, database.py)
│   │   ├── models/                # DB 테이블과 1:1 매칭되는 모델 클래스
│   │   ├── api/                   # 라우팅 계층: 도메인별 Blueprint 분리 (routes_*.py)
│   │   ├── repositories/          # DB CRUD 전담 클래스
│   │   └── services/              # 순수 비즈니스 로직 (calculator, batch_processor, discord_notifier)
│   │
│   ├── tests/
│   │   ├── fixtures/               # 테스트 입출력 데이터 (JSON/TXT)
│   │   └── test_*.py               # 객체별 1:1 전용 테스트 (test_01_... 순번 표기)
│   │
│   └── requirements.txt
│
├── data/
│   └── app.db                     # SQLite 로컬 DB (.gitignore 대상)
│
├── frontend/                      # [웹 UI] Thin Client (정적 파일)
│   ├── components/                 # PC 공통 UI 컴포넌트 (sidebar.html)
│   ├── pages/                      # PC 개별 화면 (상대경로 참조)
│   ├── css/                        # PC 스타일시트 (common.css: #sidebar-container 220px 고정)
│   ├── js/                         # PC 자바스크립트 (load_sidebar.js, api.js 등)
│   ├── mobile/                     # 모바일 전용 영역 (components/nav.html, pages, css, js)
│   └── index.html                  # 접속 기기 감지 후 PC/Mobile 분기
│
├── .env                            # 보안 환경변수
├── .gitignore
└── README.md
```
